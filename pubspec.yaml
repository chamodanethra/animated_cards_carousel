name: animated_cards_carousel
description: animated_cards_carousel is a Flutter package that provides a customizable carousel of animated cards. It allows for smooth scrolling and visually appealing animations as cards transition into and out of the viewport.
version: 1.0.0
homepage: https://github.com/chamodanethra/animated_cards_carousel
repository: https://github.com/chamodanethra/animated_cards_carousel

environment:
  sdk: '>=3.3.4 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter: